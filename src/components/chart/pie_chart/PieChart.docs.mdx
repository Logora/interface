---
description: A component that renders a pie chart related to the dataset passed.
---

import { PieChart } from './PieChart';

A component that renders a pie chart related to the dataset that is passed.

It must be inside [react-intl](https://formatjs.io/docs/getting-started/installation/)'s IntlProvider.

### Component usage

```js
import { PieChart } from '@logora/debate.chart.pie_chart';
```

```js
    const data = [{ "dimension": 115, "value": 2 }]
    const labels = [{ "id": 114, "name": "moto", "taggings_count": 0, "display_name": "moto"},{ "id": 115, "name": "honda", "taggings_count": 0, "display_name": "honda"},{ "id": 116, "name": "cbr", "taggings_count": 0, "display_name": "cbr"},{ "id": 117, "name": "chanyol", "taggings_count": 0, "display_name": "chanyol"}]
    const totalItemsCount = 2;
    const pieTitle = "# of proposals"

    <IntlProvider locale="en">
        <PieChart data={data} totalItemsCount={totalItemsCount} labels={labels} pieTitle={pieTitle} />
    </IntlProvider>
```