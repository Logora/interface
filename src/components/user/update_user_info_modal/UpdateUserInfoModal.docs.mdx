---
description: A modal to select name and language for user profile.
labels: ["modal"]
---

A modal component that allows users to select his first name, last name and language.

It must be inside [IconProvider](https://bit.cloud/logora/debate/icons/icon_provider).

### Component usage

```jsx
import UpdateUserInfoModal from '@logora/debate.modal.update_user_info_modal';

<ModalProvider>
    <IntlProvider locale="en">
        <AuthProvider>
            <UpdateUserInfoModal />
        </AuthProvider>
    </IntlProvider>
</ModalProvider>
```